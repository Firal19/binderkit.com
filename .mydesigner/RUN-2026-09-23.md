# mydesigner run — 2026-09-23, round 4, all four landing sites

Class **marketing** · task **redesign (element-comment driven, family-wide)** · direction
**A, Stage & satellites** (Firaol: "keep what's built and enhance, not overhaul").
Brief: RULES.md R1–R12 from two batches of element comments (Aidepost home; CareShop home
+ /shelf), each a rule for all four sites.

Units: house system (faces tokens, per-site metaphors); motion by the house budget;
banned and not used: hallmark, minimalist-ui, high-end-visual-design, the token generators.
The reference lane ran but was stopped before it reported; evidence for the direction came
from Firaol's own `Designs insprations/` folder (phone flanked by live cards; 3–4-across
compact tiles; thin-ruled numbered lists).

Shape: the audit + foundation workflows stalled (14 min of no writes) and were stopped at
Firaol's request ("continue without the work flow"); the work was then done directly in the
kit, one shared layer first, then Aidepost, CareShop, Cohort, Binderkit, then gates.

Gates before → after (desaudit mobile-gate, non-safe-area findings):
cohort 5 → 0 · careshop 13 (incl. 1 BLOCKER) → 1 (known consent overlap) · binderkit 1 → 0 ·
aidepost 22 → 1 MINOR. shots.mjs: green at 320/390/430/1440 on 34 routes after two fixes
(strip grid track, 16 and 52px overflow at 320). check.mjs: all features verified.

Traps met (recorded in memory `pho-round4-traps`): data-live attribute collision with
Aidepost's countdown; `body:has(#tally-bar)` ID specificity beating `html.has-ckb body`;
a grid track growing to a new child's min-content; old grid-area placements leaking into
re-used class names; WebKit abandoning a smooth scroll when a sticky header condenses.

## The finish pass (same night) — a fresh reviewer, then fixes

A fresh general-purpose reviewer (no memory of the build) graded the four live builds and
returned 15 findings. All were fixed or deliberately kept, then re-measured by
`scratchpad/r4/reverify.mjs` (28/28 pass):
- Aidepost: a tapped wallet card could sit under a hovered one (front card z-index 7); the
  claim's Confirm threw on a missing `.cell-i` (guarded); the phone claim 791 → 610px with
  the two actions side by side; the rail left /about (short page).
- Shared: rail and strip centring measured by rects (offsetLeft counted the sticky parent);
  strip counters use the tab's own label; a jump lands on the heading when the layout puts
  it far down; rails trimmed to 7–8 chapters; the rail tail is its arrow on phones; the
  ask-a-person block is a rule-bounded band (332 → 219–244px at 1440, no box, step titles
  only on phones); the open phone fold row reads "Close" instead of repeating the headline;
  the Keys control stays off touch phones.
- Binderkit: the chapter rail on every route; the right-edge index tabs are chapters; a
  facilities stepper in its own ruled table (Scale marked from three facilities).
- Cohort: the phone tab bar goes to pages; the rail link is teal; a rail on /security.
- CareShop: the used barcode retires fully; /write's receipt aside is a plain list.
- Dead JS removed (CareShop's receipt printer and groups, Aidepost's old strip centring,
  Cohort's four-button house picker). A first attempt at the removal cut two neighbouring
  functions (a helper matched the wrong closing brace); caught by check.mjs, restored from
  the snapshot, and the removal redone on section-comment boundaries.

Gates at release: check.mjs 45/50/48/45; shots.mjs --all --mobile and 1440 green on all
four; desaudit mobile-gate 0 non-safe-area findings on cohort, binderkit, aidepost, and on
careshop only the round-3 consent-card overlap on /loop (SE).
