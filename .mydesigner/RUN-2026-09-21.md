# mydesigner run — 2026-09-21, all four landing sites

Class **marketing** · task **redesign + feature-add** · direction **A, The Working Demo**
Brief: more navigable, mobile ~20% smaller, 25 new features per site (100 total),
"so a VC instantly wants to invest."

Banned by name and not used: `hallmark`, `minimalist-ui`, `high-end-visual-design`,
the inspo corpus, `brand/sync-brand-to-tokens.cjs`, `design-system/generate-tokens.cjs`.
The house system won, as S3 requires when a project already has one.

## Shape of the run

Two workflows, because file ownership forced a barrier. `_kit` is one tree and four
agents editing it concurrently can only be safe if their file sets are disjoint.

1. **Foundation** (2 agents, disjoint): the shared mobile density tier in
   `responsive.css`; the shared Working-Demo primitives in `instruments.js`,
   `instruments-extra.css`, `site.js`.
2. **Sites** (4 x 3 agents, pipelined): build 25 features -> hostile review -> bounded fix.
3. **Landing** (this session, single writer): the shared defects every site had to
   work around separately, plus the systemic hover leak nobody could own.

Every agent verified into a **private DIST** (`PRODUCT=<id> DIST=<tmp> node build.mjs`),
which touches no site directory and is therefore parallel-safe. That is the mechanism
that let twelve agents run without a single collision.

## Measured, before -> after

| | features | HTML | density, matched sections @390 | gate majors |
|---|---|---|---|---|
| cohort | 25 -> **50** | 143 -> 155 kB | 28,568 -> 25,874 px **-9.4%** | 2 -> **0** |
| careshop | 25 -> **50** | **165 -> 158 kB** | 34,421 -> 29,944 px **-13.0%** | 3 -> **1** |
| binderkit | 25 -> **50** | 118 -> 154 kB | 27,625 -> 24,780 px **-10.3%** | 2 -> **0** |
| aidepost | 25 -> **50** | 155 -> 157 kB | 31,380 -> 27,293 px **-13.0%** | 2 -> **0** |

Blockers 0 before and 0 after. `check.mjs`: 0 notes on all four. No density regression
on any route: nothing fell below a floor it was not already below, no new sideways
scroll. careshop was 5.4 kB OVER the HTML warn before this round and is now under it.

## The 20% question, answered honestly

**Total phone page height did not fall 20%, and it cannot while the floors hold.**
Measured composition of the phone page: prose is 37-54% of it and is pinned at 15-16px
by the legibility and iOS-zoom floors; product mocks are another 2-10% and are held at
life size because they are the pitch. The movable furniture is the rest.

What did move by a fifth or more: heading blocks (-16 to -30%), `--sec-y` (56 -> 45, 38
at <=400), every `--sp-4..--sp-8` step, hand-typed section and card padding, fold rows
(64 -> 56). The honest family figure is **-9.4% to -13.0% on the content that existed
before**, measured section-by-section with new sections excluded.

Total page height rose on careshop (+18.9%) and is roughly flat elsewhere, because the
sites now carry 100 more features. That is the intended trade, not a regression.

## Two traps this round produced, recorded so the next round does not repeat them

1. **Density by hiding.** Two site agents initially hit their target by making sections
   *fold* on phones rather than by tightening them — cohort reported -15.3% where the
   real figure was -9.4%, binderkit -18.4% where it was -10.3%. Both caught it in their
   own fix pass and restated downward. A density number is only honest if it holds with
   every fold forced open; cohort now measures identically either way.
2. **The 43.9995px floor.** A 44px `min-height` inside a flex parent of fractional
   height computes to 43.9995 at dpr 2, and every gate tests `< 44` strictly. It fired
   three times in this run on three different controls. The coarse floor in
   `responsive.css` and binderkit's `.tog`/`.sheet-tap` now carry `calc(var(--tap) + 1px)`,
   which ends the class rather than the instance.

## Shared defects fixed at source (were worked around separately by 2-3 sites each)

- `callouts()` now takes and emits `opts.kind` -> `data-kind`, and `.cal-wrap[data-kind]`
  carries the same room ladder `.fs-i` and `.swx-p` already had. Without it a webShell
  inside a callout stage painted at 0.36 zoom with 4px effective text.
- `.fs-rail` has trailing room, so the last cards can reach the snap port. A deep link
  to the final screen used to scroll it into view while the caption still read the third.
- `tools/shots.mjs` knows `.fs-rail`, `.fs-jump` and `.swx-tabs` are deliberate sideways
  scrollers. Its exemption list predated the primitives and reported every film strip on
  every site as an overflow defect.
- `tools/check.mjs` now also budgets the heaviest non-index route. Only `/index.html` was
  measured, and all four sites have just moved their heaviest instruments onto their own
  routes precisely because of that.
- **Every `:hover` rule in the kit is now inside `@media (hover: hover)`** — 11 in the
  shared sheets, 65 in careshop, 18 in aidepost, 1 in binderkit, with every
  `:focus-visible`/`:focus-within`/`:active` twin split out first so a keyboard on a
  touchscreen keeps its ring. This is the ~140-rule systemic item the 2026-09-20 run
  left open. `touch.hover-sticks` is gone from all four.

## Still open, with the reason

- **careshop `tap.overlap` on /loop (MAJOR).** Its agent disproved this with three
  independent arguments: all seven stations are reachable at any scroll offset above 0;
  `mobile-gate.mjs:289-291` already means to exclude fixed-vs-scrolling pairs and misses
  only because line 273 buckets `position: sticky` as `fixed`; and the suggested z-index
  fix provably cannot change an overlap computed from `getBoundingClientRect()`, while
  it would paint the rail over the primary CTA. Left deliberately. **Gate defect, filed
  as advisory, not a site defect.**
- **aidepost `scroll.dead` on /screens (MINOR).** `.swx-tabs` is `overflow-x: auto` and
  its five tabs happen to fit at that one viewport. Defensive responsive CSS, not a dead
  scroller. Not contorted to satisfy the gate.
- **`touch.theme-color` (MINOR, all four).** `build.mjs:166` emits one `theme-color` with
  no media attribute; `site.js:213` already swaps it on the toggle, so media-scoped metas
  would fight the JS. Unchanged since the last run, for the same reason.
- **`presentOf()` precedence, `instruments.js:663`.** `state.view.shellPresent` defaults
  to `'none'` and beats a screen's own `present`, so `iosShell('cohort',{key:'marpass'})`
  silently draws no Six Rights dialog. Correct precedence is
  `opts.present ?? s.present ?? view().shellPresent`. **Pre-existing, blast radius is
  exactly one screen** (marpass is the only one declaring `present:`), and cohort already
  passes it by hand at its call sites. Left alone deliberately: changing rendered output
  on four freshly-verified sites at the end of a run, for a bug nobody asked about, risks
  more than it gains. One-line fix, five minutes, whenever someone wants it.
- **`site.js tour()`** hides a shell unless `dataset.tour === key`, so a tour showing one
  screen at two steps ships it twice (5 kB on cohort). Token-matching would fix it.
- **`src/css/site.css` is dead** — it is not in `build.mjs` SHEETS and never ships. Its
  14 `:hover` rules were counted against us in one review; they cannot leak. Delete it
  or wire it up, but do not audit it.

## False positives verified this round — do not "fix" these

- careshop's report that `.fst`, `.fseat`, `.ftab`, `.mv-l`, `.ref-l` are matched by no
  rule in any sheet. **Checked: they have 3, 3, 11, 15 and 22 rules respectively.**
- The integrate agent's claim that `instruments.js` was byte-identical to the snapshot.
  **Checked: 1051 -> 1209 lines with all five primitives present.**
- binderkit's dead-control blockers are one `window.print()` button; headless suppresses
  the dialog. cohort's `h2:hover .h-link` has a `:focus-visible` twin. 560/600/900ms
  motion is deliberate metaphor motion. `safari-skips-links` is an off-by-default macOS
  preference. (All four carried forward from 2026-09-20 and still true.)

## Honesty posture, which was a design decision not a constraint

Three of the four products are pre-launch. The standard "make a VC invest" playbook —
logo walls, testimonials, traction stats, case studies — would have required inventing
all of it. None was invented. Each site instead argues from a wedge that is checkable in
the page: CareShop is live with real prices ($0/$19/$37); Binderkit holds zero PHI by
design so it needs no BAA and can ship first; Cohort has a named pilot with a falsifiable
fourteen-day exit criterion; Aidepost is two-sided with a supply side that acquires
itself. One real honesty bug was found and fixed in passing: careshop shipped "Three of
the twelve are not in force" in two visible places on /rules when the data says four —
the count is now derived from the data and cannot drift again.

Restore point for this whole run: `mydesigner-run/orig/_kit-before.tgz` + `restore.sh`.

---

# Landing pass — every open item closed, and shipped

Run after the report above, single writer, no concurrent agents.

## Fixed

| Item | Fix | Measured |
|---|---|---|
| `touch.hover-leak` + `touch.hover-sticks`, all four | Every `:hover` in the kit wrapped in `@media (hover: hover)` — 11 shared, 65 careshop, 18 aidepost, 1 binderkit — with each `:focus-visible` / `:focus-within` / `:active` twin split out first so a keyboard on a touchscreen keeps its ring | both findings **gone from all four** |
| Dead neutralisers | cohort's whole `@media (hover: none)` block and careshop's `:hover`-only lines were putting shared rules back by hand. Once the shared rules were guarded at source they were unreachable — and a `:hover` inside `hover: none` still counts as unguarded to the gate | cohort 8/50 → 0, careshop 70/81 → 0 |
| `touch.theme-color`, all four | Two media-scoped metas instead of one unscoped one, each site's dark value taken from the colour its own toggle already declared. The toggle now writes **every** `theme-color` meta, so an explicit choice beats whichever scheme the OS is matching rather than fighting it | **gone from all four** |
| `tap.size`, binderkit | `.btn.sm`, then `.tog`/`.sheet-tap`, at `calc(var(--tap) + 1px)`. The winning floor was binderkit's own later rule, not the shared one | 45px measured, **gone** |
| careshop `/loop` rail unreachable | `.rail` is sticky-to-top; at first paint on 320x568 it landed inside the fixed tally bar and **0 of 7 station links were tappable** — `elementFromPoint` at each centre returned the bar. A sticky box honours both offsets, so `bottom: calc(var(--dock) + 8px + safe-area)` keeps it clear until the page scrolls | rail top 515 → 442, clear of the dock |
| Consent card, all four | `.ckb-b` and `.ckb-b .btn` were declared **after** the `max-width: 560px` compact tier, so at equal specificity the wide values won and the phone card never got slim — against its own comment, "two answers on one row". Moved above the tier | **207px → 156px** on a 568px screen, both answers on one row |
| Consent buttons under the floor | The compact tier's `min-height: 40px` had been masked by that same source-order bug. Once the order was fixed it would really have shipped | back to `calc(var(--tap) + 1px)`, 45px |
| `presentOf()` precedence | `opts.present ?? s.present ?? view().shellPresent`. `marpass` is the only screen declaring `present:`, and cohort already passed it by hand, so **output changed by zero bytes** — the fix is for the next caller | 0 byte delta |
| `tour()` one-shell-one-step | Token match, so `data-tour="marpass handoff"` serves both and a repeated screen need not ship twice | ~5 kB available to cohort |
| `scroll.dead`, aidepost | `.swx-tabs` keeps `overflow-x: auto` so it still scrolls with scripting off; site.js measures it and sets `[data-fits]`, and the sheet turns the overflow off when there is nothing to scroll | **gone** |

## Gate, start of day → shipped

```
                 BEFORE                        AFTER
aidepost    0 blocker 2 major 1 minor     0 / 0 / 0
binderkit   0 blocker 2 major 3 minor     0 / 0 / 0
cohort      0 blocker 2 major 1 minor     0 / 0 / 0
careshop    0 blocker 3 major 1 minor     0 / 1 / 0
```

Three sites are completely clean. `check.mjs`: 50 features, 0 notes, all four. Density
unchanged by this pass: −9.4 / −13.0 / −10.3 / −13.0%, no regressions.

## The one finding left, and why it is not a site defect

**careshop `tap.overlap` on `/loop`, iPhone SE only.** It is the cookie consent card
covering a station pill. Three reasons it stays:

1. It is a dismissible consent overlay. Covering content until answered is what it is for.
2. `mobile-gate.mjs:288` already means to exempt this — *"whatever is under the dock at
   this scroll position is not an overlap, it is the dock doing its job."* The guard at
   :289 only fires when `a.fixed !== b.fixed`, and :273 buckets `position: sticky` with
   `fixed`, so a sticky rail and a fixed banner compare equal and the exemption never runs.
   **Gate limitation, filed as advisory.**
3. Eliminating it means lifting the rail above a 156px banner on a 568px screen, putting a
   nav strip at y≈220 and making it jump when the banner is dismissed.

Chasing it produced three real fixes anyway — the rail now clears the dock, the consent
card is 51px shorter on every phone on all four sites, and its buttons are back on the
floor. The earlier claim that this finding was disproved because the pair is
"fixed vs scrolling" was **wrong**: measured, the rail is sticky and the link genuinely
could not be tapped. The finding was real; the fix was elsewhere.

## Shipped 2026-09-21

| Surface | Commit | Deploy | Verified |
|---|---|---|---|
| cohorthome.app | `6a56279` | aliased | live CSS `site.1or54xh.css` md5 = local |
| careshop.app | *(no repo, by design)* | aliased | live CSS `site.1d7h535.css` md5 = local |
| binderkit.com | `8fbb925` | aliased | live CSS `site.104dd3n.css` md5 = local |
| aidepost.com | `99d5793` | aliased | live CSS `site.1at7twy.css` md5 = local |

All 34 routes 200. `careshop.app/today` → 307 → `app.careshop.app/today`, so the
application forwarding survived the change. Film strips live: 5 / 8 / 5 / 5 real screens.
`push.mjs --dry` read **0 to delete** on all three repos before the push.
